# Task 3: De-Stainless documentation removal Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Remove all stale Stainless documentation from the fork: generation headers, generation claims in docs, and the `.stats.yml` Stainless-tooling artifact (repointing the prism scripts at the local spec).

**Architecture:** The fork is hand-maintained from `spec/openapi-opencode.json`; nothing is generated by Stainless anymore. Stale claims live in: per-file headers (`# File generated from our OpenAPI spec by Stainless...`) across `src/` and `tests/`, README's "It is generated with Stainless", CONTRIBUTING's workflow, api.md's header, PUBLISHING.md, and `.stats.yml` (read by `scripts/mock` and `scripts/test` to locate the prism spec). Historical mentions in `CHANGELOG.md` stay.

**Tech Stack:** Python (bulk header rewrite via script), git, uv venv.

## Global Constraints

- Branch `feat/1.18-api-surface`; commit ONCE at the end (grouped, D9).
- Repo root `/workspaces/opencode-sdk-python`.
- Prose edits (README, CONTRIBUTING, api.md, PUBLISHING) go through `catalyst-v2-writing-docs` (style rules + mandatory humanizer pass). Invoke that skill before writing prose.
- Do NOT touch `uv.lock`, `pyproject.toml`, or other tasks' files.
- `CHANGELOG.md` keeps its historical Stainless mentions (it records history). The `@stainless-api/prism-cli` npm package name in `scripts/mock` is upstream tooling, NOT stale docs - keep it.
- Skip project-wide lint/tests; only the gates below.

---

### Task 3: Remove stale Stainless docs

**Files:**
- Modify: headers in `src/opencode_ai/**/*.py` and `tests/**/*.py` (bulk)
- Modify: `README.md`, `CONTRIBUTING.md`, `api.md`, `PUBLISHING.md` (check-and-fix)
- Delete: `.stats.yml`
- Modify: `scripts/mock`, `scripts/test` (repoint spec default)
- Check: `docs/`, `noxfile.py`, `Brewfile` for stale claims; fix if found

**Interfaces:**
- Consumes: current file contents (read them first), `spec/openapi-opencode.json` (the local tracking spec).
- Produces: no "generated by Stainless" claims anywhere live; `scripts/mock` and `scripts/test` use `spec/openapi-opencode.json` by default.

- [ ] **Step 1: Inventory every Stainless mention**

Run: `grep -rn "Stainless" --include="*.py" --include="*.md" --include="*.toml" --include="*.yml" . | grep -v "^./.git/" | grep -v "^./.cortex/" | grep -v "CHANGELOG.md"`
Expected: a complete list. Classify each hit: header line, prose claim, workflow reference, or historical. The `@stainless-api/prism-cli` package name and CHANGELOG are exempt (see Constraints).

- [ ] **Step 2: Rewrite the file headers (bulk, uniform)**

The header line `# File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.md for details.` becomes:

`# Hand-maintained fork of the Stainless-era opencode SDK. Generated code was replaced by manual maintenance; see CONTRIBUTING.md.`

Do this with one scripted pass over `src/opencode_ai/**/*.py` and `tests/**/*.py` (e.g. a small python or perl one-liner). Verify: `grep -rln "File generated from our OpenAPI spec by Stainless" src tests | wc -l` -> 0. Do not reformat anything else in those files.

- [ ] **Step 3: README.md**

Read the README; replace the "It is generated with Stainless." sentence with a maintenance statement (e.g. "Hand-maintained from the opencode OpenAPI spec (`spec/openapi-opencode.json`), tracked to the opencode release line."). Check the whole README for other generation claims; fix with the writing-docs skill. Do not restructure the README.

- [ ] **Step 4: CONTRIBUTING.md**

Read it. Replace any Stainless/codegen workflow with the hand-maintenance workflow: edit `src/opencode_ai/resources/*` and `types/*` from the spec, regenerate api.md via `uv run python scripts/gen_api_expanded.py`, run `uv run ruff check .`, `uv run mypy .`, `uv run pyright`, `uv run pytest`. Keep existing contributing guidance that still applies. Writing-docs pass.

- [ ] **Step 5: api.md and PUBLISHING.md**

Check api.md's header and PUBLISHING.md for generation claims; fix with writing-docs. api.md's body (method docs) stays as-is in this task - resource tasks 4-6 extend it.

- [ ] **Step 6: Delete .stats.yml and repoint the prism scripts**

Delete `.stats.yml`. In `scripts/mock`, replace the `openapi_spec_url` grep fallback with a default of `spec/openapi-opencode.json` (keep the ability to pass an explicit URL/path as the first argument - check how the current script uses `$1` and preserve that behavior). In `scripts/test`, check the error message and the spec source reference; make it point at the local spec path consistently. Verify: `grep -n "openapi_spec_url\|stats.yml" scripts/` -> no hits.

- [ ] **Step 7: Verify the gates**

Run:
`grep -rn "Stainless" README.md CONTRIBUTING.md api.md PUBLISHING.md scripts/ docs/ noxfile.py Brewfile 2>/dev/null` -> no hits (or only the npm package name)
`grep -rln "File generated from our OpenAPI spec by Stainless" src tests | wc -l` -> 0
`nix develop --command uv run pytest tests/test_client.py tests/api_resources/test_mcp.py -n 0 -q` -> PASS (proves the touched tooling/docs did not break the suite surface)
`git diff --stat` -> headers + 5 docs + scripts + deleted .stats.yml, nothing else

- [ ] **Step 8: Commit (grouped, D9)**

```bash
git add -A src tests README.md CONTRIBUTING.md api.md PUBLISHING.md scripts/mock scripts/test
git rm .stats.yml
git commit -m "docs: remove stale Stainless generation docs, repoint prism scripts at local spec"
```

Expected: ONE commit. If `scripts/mock`/`scripts/test` edits belong with the deletion, they are in this same commit.

**Acceptance (inviolable):** step 7 gates pass; one grouped commit; no live "Stainless generation" claims remain (CHANGELOG and the npm package name exempt).

**Report format:** a diff: `git show --stat HEAD`, the inventory table (hit -> action), gate outputs. If a gate cannot pass, STOP and report with output intact - do not descope.
